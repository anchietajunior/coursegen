package prompt

// DefaultTemplate returns the embedded default prompt template source, so
// `coursegen init` can scaffold an editable copy into the project.
func DefaultTemplate() string { return defaultTemplate }
